import * as test from 'blue-tape';

// import grafanaSdkTest = require('grafana-sdk-test');

test('simple delay', function(t) {
    // return (1);
    t.ok(1,'Im feeling much better now!');
    t.end();
});
